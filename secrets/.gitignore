# Everything in this folder is secret. Ignore it all, including this rule's
# own siblings, but keep the .gitignore and README so the folder's purpose
# survives a fresh clone.
*
!.gitignore
!README.md
