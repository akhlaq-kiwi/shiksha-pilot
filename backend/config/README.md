# Firebase Configuration Directory

Place your entire downloaded Firebase Service Account JSON file in this directory with the filename:

`firebase-service-account.json`

Target Path:
`backend/config/firebase-service-account.json`

> [!NOTE]
> The `firebase-service-account.json` file is automatically ignored by `.gitignore` so your private service account key will never be committed to git.
